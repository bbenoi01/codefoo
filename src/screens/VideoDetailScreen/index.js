import { connect } from 'react-redux';
import VideoDetailScreen from './VideoDetailScreen';

function mapStoreToProps(store) {
    return {

    }
}

export default connect(mapStoreToProps)(VideoDetailScreen);