import { connect } from 'react-redux';
import ArticleDetailScreen from './ArticleDetailScreen';

function mapStoreToProps(store) {
    return {

    }
}

export default connect(mapStoreToProps)(ArticleDetailScreen);